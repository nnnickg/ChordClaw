[workspace]
members = [
    "crates/chordclaw-core",
    "crates/chordclaw-cli",
    "crates/chordclaw-wasm",
]
resolver = "2"

[workspace.package]
version = "1.5.1"
edition = "2024"
license = "MIT"
repository = "https://github.com/nnnickg/chordclaw"
homepage = "https://github.com/nnnickg/chordclaw"

[workspace.dependencies]
chordclaw-core = { path = "crates/chordclaw-core", version = "1.5.1" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
clap = { version = "4.6.1", default-features = false, features = ["std", "help", "usage", "error-context"] }
clap_complete = "4.6.1"
# Pinned to the exact patch: the generated JS glue must match the installed
# wasm-bindgen-cli version, and a floating minor invites mismatch build failures.
wasm-bindgen = "=0.2.126"

[workspace.lints.rust]
unsafe_op_in_unsafe_fn = "deny"
unused_must_use = "deny"

[workspace.lints.clippy]
dbg_macro = "deny"
expect_used = "deny"
panic = "deny"
todo = "deny"
unimplemented = "deny"
unreachable = "deny"
unwrap_used = "deny"

[profile.release]
codegen-units = 1
lto = "thin"
strip = "symbols"

[profile.dev.package.chordclaw-core]
opt-level = 2
